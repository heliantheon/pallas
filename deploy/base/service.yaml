apiVersion: v1
kind: Service
metadata:
  name: pallas
  labels:
    app.kubernetes.io/name: pallas
    app.kubernetes.io/part-of: heliantheon
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: pallas
  ports:
    - name: http
      port: 8080
      targetPort: http

